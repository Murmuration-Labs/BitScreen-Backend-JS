exports.addCid = ( bitScreenId, contentId ) => {
    console.log(`Inserting new Content Id, ${contentId}, for Bitscreen ${bitScreenId}` )
}

exports.addBitscreen = ( bitScreenId ) => {
    console.log(`Inserting new Bitscreen ${bitScreenId}` )
}