exports.updateCid = ( bitScreenId, contentId ) => {
    console.log(`Updating content ID,  ${contentId}, for bitscreen, ${bitScreenId}`)
}

exports.updateBitScreen = ( previousBitscreen, newBitscreen ) => {
    console.log(`Previous ${previousBitscreen}, new bitscreen name, ${newBitscreen}`)
}