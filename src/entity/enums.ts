export enum Visibility {
  None,
  Private,
  Public,
  Shared,
  Exception,
}

export enum Sources {
  Bitscreen = "bitscreen",
  Rodeo = "rodeo",
}

export enum AnalysisService {
  Safer = 'safer',
  Thorn = 'thorn'
}
